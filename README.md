# W6-32 Information

This repository provides information which caan be used for protein design work to generate a nanobody which mimics the qualities of the W6/32 antibody, but without the steric clashes involving the light chain and the linkers in the Single Chain Trimer.

# Use of AI in this project

One aim of this project is to understand how Agentic AI can be used in scientific research. The project was initially proposed as part of the AI Scientists call by ARIA but was sadly not funded. 

One aim of this repository, aside from the generation of data, is to show how Claude Code can build tools and compose them into analysis, keep a lab notebook and then generate conclusions. 

Minimal intervention with Claude Code was performed during the generation of tools, their use and subsequent analysis. 

The initial experimental design is contained in the EXPERIMENTAL_DESIGN.md file. Claude Code's plan is in PLAN.md.

The lab notebook is the CHANGELOG.md file. 

The conclusions from the experimental work generated by Claude Code are in the CONCLUSIONS.md file.

# Versions used

Claude Opus 4.8 on "high effort" mode.

# Time taken for the process

From assembling the structures, writing the experimental design document to completion of the tasks was about 4 hours.


# Human in the Loop

I designed the experiment and suggested the ;ibraries to use

I wrote the EXPERIMENTAL_DESIGN document

I assembled the structures and ran predictions for Single Chain Trimers

I responded to planning stages and for tool use when asked and approved all Git commit messages.

I said 'please' and 'thank you' as we need to model good behaviour

# Tokens used

Summary generated by Claude.

Model claude-opus-4-8, 326 assistant turns

Output (generated by Claude)	490,314
Input — fresh (non-cached)	23,183
Input — cache writes	1,277,845
Input — cache reads	50,816,411
Grand total (all tokens)	52,607,753

- Output ≈ 490K — the text/code Claude actually generated. The most "honest" measure of work produced.
- Cache reads ≈ 50.8M dominate the total — that's the running context (files, history, tool results) re-read each turn. - It's billed at a steep discount (~10% of input price), so the raw 52.6M total is not 52.6M at full price.
- ~1.79M = input + output + cache writes excluding cache reads — a rough "non-cached" figure.

On a Claude Max plan this was 19% of my Session usage (5h) and 5% of my Weekly usage (7 day) quotas. 

# Date work performed

June 5th 2026

